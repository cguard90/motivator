<%= form_for [@goal, @message] do |f| %>
  <fieldset>
    <%= f.text_area :content %>
    <%= submit_tag "Post your message", class: 'button-user' %>
  </fieldset>
<% end %>

<% if @errors %>
  <% @errors.each do |error| %>
    <%= error %>
  <% end %>
<% end %>
