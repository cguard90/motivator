<%= form_for [@goal, @pledge] do |f| %>
  <fieldset>
    <div>
      <%= f.label :amount %>
      <%= f.number_field :amount, min:1, :placeholder => "$" %>
    </div>
    <%= f.submit 'Add Pledge'%>
  </fieldset>
<% end %>




<% if @errors%>
  <% @errors.each do |error|%>
    <%= error %>
  <% end %>
<% end %>